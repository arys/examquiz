<%= simple_form_for(@game) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
  	<%= f.input :opponent, label: 'Введите username:', placeholder: 'Оставьте пустым если хотите выбрать пользователя online'%>
  </div>
  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
